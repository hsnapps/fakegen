<div id="{{ $name }}_div">
    <label class="uk-form-label" for="{{ $name }}">{{ __('app.'.$name) }}</label>
    <div class="uk-form-controls">
        <input class="uk-input" id="{{ $name }}" name="{{ $name }}" type="text" placeholder="{{ __('app.'.$name ) }}" autocomplete="off">
    </div>
</div>
